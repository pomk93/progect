//const myName = 'Bogdan' //будет ошибка если я запущю ее в модуле moduleOne

const sum = (a, b) => a+b; //Здесь не явный возрат значения фигурные скобки не нужны

const mult = (a,b) => a*b;
export {
    sum, 
    mult

};
